// todo: implement test (preferably with 'jest')

/* atm/withdrawel route tests
// it returns bills and coins correctly for withdrawal amount
// it returns bills and coins correctly for decimal withdrawal amount
// it returns 400 when withdrawal amount is negative
// it returns 400 when withdrawal amount is invalid (not a number)
// it returns 409 (conflict) when over max amount available for withdrawal
// it throws TooMuchCoinsException when withdrawal has more than 50 coins
*/